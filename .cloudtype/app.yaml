# Optional CLI reference. Dashboard: Dockerfile template, path Dockerfile.cloudtype, port 3100.
name: paperclip
app: dockerfile
options:
  ports: 3100
  dockerfile: Dockerfile.cloudtype
context:
  preset: dockerfile
  git:
    branch: master
