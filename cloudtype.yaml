# Optional: Cloudtype CLI (`ctype apply`) reference only. Dashboard Git deploy does not read this file.
name: paperclip
app: dockerfile
options:
  ports: 3100
  dockerfile: Dockerfile.cloudtype
context:
  preset: dockerfile
  git:
    branch: master
